{
  "name": "pheasant",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -t babelify -o build/app.js",
    "server": "http-server -a localhost -p 9999",
    "watch": "watchify index.js -t babelify -o build/app.js --debug --verbose"
  },
  "author": "",
  "license": "ISC"
}
